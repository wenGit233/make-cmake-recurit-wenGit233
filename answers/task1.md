# Task 1：从 C 源码到可执行文件

请用自己的语言简要说明下面几个阶段分别做了什么：

1. 预处理
1) 处理头文件.h
eg:把.h文件中代码复制此处，方便后续函数函数调用
2) 处理宏定义(纯文本替换，eg定义MAX常量不占内存)
3) 条件编译  
由条件判断代码被保留or剔除


2. 编译
* 把预处理后的代码转换为汇编代码检查大量语法与类型错误。
* 作用？适配早期编译器，便于直接在汇编语言上修改，更灵活，模块化减少重新编译工作量。 


3. 汇编
转化为计算机可以识别的二进制语言
4. 链接
将二进制语言转化为可执行程序。包含符号解析，重定位，库链接。

