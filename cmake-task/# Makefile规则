# Makefile规则
指令前必须打一个[Tab]键
执行make clear指令后，将只剩下hello.c 和 hello可执行文件
TAR = test，就表示下面的代码中，我们可以用TAR代表test文件。
如果再写TAR += test1，就表示TAR代表test和test1。
CC := gcc 就表示下面写gcc的地方全部可以用CC代替，因为gcc这个是不会变的，是常量，所以可以用恒等于替换，这个不能用+=
.PHONY:这是固定格式，不能变的，但是下面的clear是自己取的名字
# 目标文件：test
# 现有文件：program1.c program1.h program2.c program2.h main.c main.h

test:program1.o program2.o main.o
	gcc program1.o program2.o main.o -o test

program1.o:program1.c
	gcc -c program1.c -o program1.o

program2.o:program2.c
	gcc -c program2.c -o program2.o

main.o:main.c
	gcc -c main.c -o main.o

.PHONY:
clean_all:
	rm program1.o program2.o main.o
=
替换
+=
追加
:=
恒等于  
%.o
任意的.o文件
*.o
所有的.o文件
-Wall
W = Warning，all = all。开启大部分编译器警告。
代码有隐患、不规范，gcc 就会提醒你，写代码强烈推荐加上。
2. -Wextra
在 -Wall 的基础上，再多开启额外的警告检查。
小总结：-Wall -Wextra 就是让编译器严格一点，帮你找 bug。
3. -Iinclude
大写的 -I（i），作用：添加头文件搜索目录
TAR = test
OBJ = program1.o cube.o main.o
CC := gcc
RMRF := rm 

$(TAR):$(OBJ)
	$(CC) $^ -o $@
%.o:%.c
	$(CC) -c $^ -o $@

.PHONY:
cleanall:
	$(RMRF) $(OBJ)
make 通过**文件时间戳**判断是否需要编译。`calculator.c` 时间变新了，  
make 认为源码发生变化，所以重新编译这个 `.c` 文件得到 `.o`，再重新链接；其他源码时间没变，就不编译。
33%：编译 src/main.c → 生成 main.c.o（目标文件）
2. 66%：编译 src/calculator.c → 生成 calculator.c.o
3. 100%：把两个.o文件链接在一起，生成最终可执行文件 calculator
CMake 底层生成 Makefile，和最开始 make 原理一样：
CMake/Make 会对比源码文件和编译出来的.o 目标文件的修改时间：
• 如果源码比.o新 → 重新编译这个源码
• 如果源码没有改动 → 跳过，复用旧的.o文件（增量编译，节省时间）
这里touch src/calculator.c把calculator.c的时间改成最新，所以它对应的.o需要重新生成；main.c没变，所以不编译。
你可以试一下反过来验证
bash
￼
￼
￼
touch
 src/main.c
cmake 
--build
 build
￼
这时就只会编译main.c，不再编译calculator.c。